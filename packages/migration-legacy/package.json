{
  "name": "@byf/migration-legacy",
  "version": "0.0.1",
  "description": "Legacy migration tool (to be removed).",
  "license": "MIT",
  "type": "module",
  "private": true,
  "files": [
    "dist"
  ],
  "imports": {
    "#/*": [
      "./src/*.ts",
      "./src/*/index.ts"
    ]
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@byf/agent-core": "workspace:^",
    "smol-toml": "^1.6.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@byf/kaos": "workspace:^"
  }
}
