Overwrite or append to a file with content exactly as provided, creating the file if needed; the parent directory must already exist. Defaults to overwrite; append adds content to the end without adding a newline. Overwriting an existing file you haven't Read in this session will fail — Read it first, or use Edit for partial changes. Write does not use the Read/Edit model text view and does not preserve or infer the previous line-ending style: \n stays LF, \r\n stays CRLF. When the content is very large, split it across multiple calls: write the first chunk with overwrite, then add the remaining chunks with append.
